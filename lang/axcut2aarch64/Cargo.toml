[package]
name = "axcut2aarch64"
version = "0.0.1"
edition = "2021"

[dependencies]
axcut = { path = "../axcut" }
axcut2backend = { path = "../axcut2backend" }
printer = { path = "../printer" }

[dev-dependencies]
goldenfile = { version = "1.7.3" }
