[package]
name = "axcut2backend"

# Inherited from workspace Cargo.toml
version.workspace = true
edition.workspace = true

[dependencies]
axcut = { path = "../axcut" }
printer = { path = "../printer" }
