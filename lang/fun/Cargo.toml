[package]
name = "fun"
version = "0.0.1"
edition = "2021"

[dependencies]
lalrpop-util = { version = "0.20.2", features = ["lexer"] }
# Deriving
derivative = { workspace = true }
# Source code locations
codespan = { workspace = true }
# Error messages
thiserror = { workspace = true }
miette = { workspace = true }

[build-dependencies]
lalrpop = "0.20.2"

[dev-dependencies]
# For generating the integration / parser tests
test_each_file = { version = "0.3.3" }
